# Base URL of the KSB instance to verify (the live testnet 12 deployment
# for a Phase 9 run, or a local instance for a smoke check).
KSB_BASE_URL=http://localhost:3000

# Operator key. The harness registers its own app and drives every flow.
KSB_OPERATOR_API_KEY=
