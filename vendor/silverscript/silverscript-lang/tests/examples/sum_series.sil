contract SumSeries(int maxIterations){
    entrypoint function main(int n){
        require(n >= 0);
        require(n <= maxIterations);
        int s = 0;
        for(i,1,n+1,maxIterations){
            s = s + i;
        }
        require(s == n*(n+1)/2);
    }
}