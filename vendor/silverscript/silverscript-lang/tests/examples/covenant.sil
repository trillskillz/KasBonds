pragma silverscript ^0.1.0;

contract Covenant(int requiredVersion) {
    entrypoint function spend() {
        require(tx.version == requiredVersion);
        require(this.activeScriptPubKey == byte[](0x00));
    }
}
