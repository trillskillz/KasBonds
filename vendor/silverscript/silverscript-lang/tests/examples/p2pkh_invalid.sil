pragma silverscript ^0.1.0;

contract P2PKH(byte[20] pkh) {
    entrypoint function spend(pubkey pk, sig s) {
        require(byte[](blake2b(pk)) == byte[](s));
        require(checkSig(s, pk));
    }
}
