pragma silverscript ^0.1.0;

contract Slice(byte[20] pkh) {
    entrypoint function spend() {
        byte[] actualPkh = tx.inputs[this.activeInputIndex].scriptPubKey.slice(3, 23);
        require(byte[](pkh) == actualPkh);
    }
}
