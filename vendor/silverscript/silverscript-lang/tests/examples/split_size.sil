pragma silverscript ^0.1.0;

contract SplitSize(byte[] b) {
    entrypoint function spend() {
        byte[] x = b.split(b.length / 2).1;
        require(x != b);
        require(byte[](b.split(4).0) != x);
    }
}
